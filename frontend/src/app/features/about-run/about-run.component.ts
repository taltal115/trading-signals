import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-about-run',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './about-run.component.html',
})
export class AboutRunComponent {}
