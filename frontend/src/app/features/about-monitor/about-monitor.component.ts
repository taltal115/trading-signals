import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-about-monitor',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './about-monitor.component.html',
})
export class AboutMonitorComponent {}
